anthropic>=0.40.0
pydantic>=2.0.0
python-dotenv>=1.0.0
ddgs>=9.0.0                    # DuckDuckGo web search (free, no API key)
rich>=13.0.0                   # Rich terminal output (tables, panels, spinners)
